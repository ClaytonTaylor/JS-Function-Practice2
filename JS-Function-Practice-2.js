// #1

/*

function getName (name) {
	var description = {
		name: "luisa",
		age: 25,	
	};
	return console.log(description.name)
};

getName();


*/

// #2

/*

var strings = ["Clay" , "Taylor"];

function totalLetters (number) {
	var count = 0;
	for(var i = 0; i < strings.length; i++) {
		count +=  (strings[i].length);
	}

	return count
}

totalLetters(strings);


*/

// #3

/*

function keyValue (city, name) {
	var keyCity = "city: ";
	var keyName = "Denver";
	var Obj = {
		name: keyCity,
		city: keyName
	}
	return Obj.name + Obj.city
}

keyValue();

*/

// #4

/*

function negativeIndex (somearray, number) {
	var array = ["apple", "betty", "cat", "dog", "egg"]
	return array[array.length-3]
}

negativeIndex();

*/

// #5

/*

function removeM (string) {
	var word = prompt ("Please enter a word with an 'M'.");
	return word.replace('m','')
	
}
removeM();

*/

// #6

